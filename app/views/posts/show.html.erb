<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <% if @post.file? %>
    <%= image_tag image_path(@post.id) %>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
